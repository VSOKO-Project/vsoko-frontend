stages:
  - build
  - deploy

variables:
  DOCKER_IMAGE_NAME: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG

build_image:
  stage: build
  image: docker:27.5.0
  services:
    - docker:27.5.0-dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
      - docker builder prune -f --filter unused-for=24h
      - docker build --pull
        --build-arg VITE_API_BASE_URL=$VITE_API_BASE_URL
        --build-arg VITE_DEFAULT_PAGE_SIZE=$VITE_DEFAULT_PAGE_SIZE
        -t $DOCKER_IMAGE_NAME .
      - docker push $DOCKER_IMAGE_NAME
  only:
    - main
    - stage

.deploy_template:
  stage: deploy
  image: docker:27.5.0
  before_script:
    - apk add --no-cache openssh-client
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | base64 -d | ssh-add -
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\tPort $TARGET_PORT" > ~/.ssh/config
    - docker context create remote-target --docker "host=ssh://$TARGET_USER@$TARGET_HOST"
  script:
    - ssh $TARGET_USER@$TARGET_HOST "mkdir -p $INFRA_PATH"
    - scp docker-compose.yml $TARGET_USER@$TARGET_HOST:$INFRA_PATH/docker-compose.yml
    - ssh $TARGET_USER@$TARGET_HOST "
        export CI_REGISTRY_IMAGE=$CI_REGISTRY_IMAGE DOCKER_TAG=$CI_COMMIT_REF_SLUG &&
        cd $INFRA_PATH &&
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        (docker network inspect web_network >/dev/null 2>&1 || docker network create web_network || docker network inspect web_network >/dev/null) &&
        docker compose pull &&
        docker compose up -d --no-build --remove-orphans &&
        docker image prune -f
      "

deploy_stage:
  extends: .deploy_template
  variables:
    INFRA_PATH: "$BASE_DEPLOY_PATH/frontend"
    TARGET_PORT: $SSH_PORT_STAGE
    TARGET_HOST: $SSH_HOST_STAGE
    TARGET_USER: $SSH_USER_STAGE
  environment:
    name: staging
  only:
    - stage

deploy_prod:
  extends: .deploy_template
  variables:
    INFRA_PATH: "$BASE_DEPLOY_PATH/frontend"
    TARGET_PORT: $SSH_PORT_PROD
    TARGET_HOST: $SSH_HOST_PROD
    TARGET_USER: $SSH_USER_PROD
  environment:
    name: production
  only:
    - main
